package 문자열기본;

public class _03문자열비교 {
	public static void main(String[] args) {
		
		// String name ~~~
		// name.equals(~) 를 하면 주소값을 제외하고 순수 값만을 비교해 준다.
		// compareTo : char 의 숫자 특성을 이용해서 문자열이 같은지 다른지를 비교
		// name.compareTo(~) == 0 ? true : false;
		// char 타입을 int 로 형변환 하여 값을 비교해 주는 것과 같은 원리.
		
	}
}
